# ===== DEBUG TEST 1: vérifier date_pose ligne 137/138 (index pandas) =====
for idx in [137, 138]:
    if idx < len(df_final):
        dp = df_final.loc[idx, "date_pose"]
        print(f"[DEBUG] row {idx} date_pose pandas = {dp} (type={type(dp)})")
