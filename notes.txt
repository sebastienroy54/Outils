library(stringr)
library(readr)

# Liste des fichiers à tester
fichiers <- c("SSR16_Anohosp.fic", "SSR22_Anohosp.fic", "SSR90_Anohosp.fic")

# Boucle sur chaque fichier
for (fichier in fichiers) {
  cat("\n📄 Fichier :", fichier, "\n")
  
  if (!file.exists(fichier)) {
    cat("⚠️ Fichier introuvable\n")
    next
  }
  
  # Lire les lignes
  lignes <- read_lines(fichier)
  lignes <- str_trim(lignes)
  
  # Extraire le format (positions 17 à 19)
  formats <- sapply(lignes, function(x) paste0("V", substr(x, 17, 19)))
  
  # Afficher la distribution des formats
  print(table(formats))
}
