SELECT
    sp.id_patient,
    COUNT(DISTINCT s.service_referent) AS nb_services
FROM soins_pall sp
JOIN sejours s
    ON sp.id_sejour = s.id_sejour
WHERE s.service_referent IS NOT NULL
GROUP BY sp.id_patient
HAVING COUNT(DISTINCT s.service_referent) > 1
ORDER BY nb_services DESC, sp.id_patient;
