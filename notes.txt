version: "3.9"

services:
  streamlit:
    build: .
    container_name: portail-outils-dev
    ports:
      - "8501:8501"

    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
    
    command: streamlit run app.py
