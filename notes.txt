print("Pose min/max:", sedi_dt_pose.min(), sedi_dt_pose.max())
print("Nb dates NaT:", sedi_dt_pose.isna().sum())
print("Répartition mois:", sedi_dt_pose.dropna().dt.to_period("M").value_counts().sort_index())
