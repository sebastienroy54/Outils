hostname -f
cat /etc/debian_version
whoami
uptime

docker --version
docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Ports}}\t{{.Status}}'
docker network ls
docker volume ls

# Ports qui écoutent (très important)
ss -lntp

# Si un reverse proxy existe (nginx/apache)
systemctl is-active nginx || true
systemctl is-active apache2 || true
