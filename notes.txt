print("LIGNES (detail):", len(detail))
print("PATIENTS UNIQUES (detail):", detail["NIP"].astype("string").str.strip().dropna().nunique())
print("SOMME des patients par libellé:", detail.groupby("Libellé DMI")["NIP"].nunique().sum())
