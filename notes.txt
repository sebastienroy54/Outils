docker run --rm \
  -v /home/github:/github \
  monouhcd-image \
  python3.7 - << 'EOF'
import yaml
from pprint import pprint
from pathlib import Path

p = Path("/github/stream/storage/query/dsn/simpa_cch.yml")
cfg = yaml.safe_load(p.read_text())

print("\n=== CLÉS NIVEAU RACINE ===")
pprint(cfg.keys())

print("\n=== CLÉS doctrine ===")
pprint(cfg.get("doctrine", {}).keys())

print("\n=== CONTENU dbal ===")
pprint(cfg.get("doctrine", {}).get("dbal"))
EOF
