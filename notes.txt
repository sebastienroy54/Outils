def read_csv_sag_safe(path):
    return pd.read_csv(
        path,
        sep=";",
        engine="python",
        encoding="cp1252",
        on_bad_lines="warn"  # ou "skip" si tu veux ignorer silencieusement
    )
